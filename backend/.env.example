# Used for local development (uvicorn run directly, not via docker-compose).
# docker-compose.yml sets its own values in the `environment:` block for each service.

DATABASE_URL=sqlite:///./security.db
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
DETECTION_THRESHOLD=0.5
